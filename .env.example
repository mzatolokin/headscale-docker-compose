# ===== ACME / Traefik =====
# Email for Let's Encrypt notifications and certificate registration
ACME_EMAIL=admin@example.com

# Set to true to use DNS-01 challenge with Cloudflare.
# Requires a token in secrets/cloudflare_api_token
TRAEFIK_ACME_DNS01=false

# Traefik log level: DEBUG, INFO, WARN, ERROR
TRAEFIK_LOG_LEVEL=INFO


# ===== Headscale =====
# Public FQDN used by Traefik router and server_url
HEADSCALE_FQDN=headscale.example.com

# Headscale base domain for MagicDNS
HEADSCALE_BASE_DOMAIN=example.com

# Public URL of Headscale (must be https and match FQDN)
HEADSCALE_SERVER_URL=https://headscale.example.com

# Use external Postgres when set; otherwise set HEADSCALE_USE_SQLITE=true
# DSN example: postgres://USER:PASSWORD@HOST:5432/DBNAME?sslmode=disable
HEADSCALE_DATABASE_URL=

# Set true to use sqlite at data/headscale/db.sqlite
HEADSCALE_USE_SQLITE=true


# ===== Image tags (pin versions) =====
# Pin image tags here for reproducible deployments
TRAEFIK_IMAGE_TAG=v2.10.7
HEADSCALE_IMAGE_TAG=v0.26.1